apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: flm
spec:
  selector:
    app: db
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None   # headless service for StatefulSet
